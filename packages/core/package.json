{
  "name": "@always-on-agent/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "agent:run": "tsc -p tsconfig.json && node dist/cli.js",
    "build": "tsc -p tsconfig.json",
    "prebuild": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "test": "vitest run src"
  },
  "devDependencies": {
    "vitest": "latest"
  }
}
